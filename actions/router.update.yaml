#TODO Router UPDATE
---
description: Router Update
enabled: true
entry_point: src/router.py
name: router.update
parameters:
    submodule:
        default: router_update
        immutable: True
        type: string
    router:
        description: Router to update (name or ID)
        required: true
        type: string
    update_kwargs:
        description: Router args to update
        required: true
        type: object
runner_type: python-script